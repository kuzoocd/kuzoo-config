# metadata
cluster: my-first-cluster
namespace: gvbusa
chart:
  type: example
  version: 0.1.6

# app config
replicaCount: 2

image:
  regex: .*

service:
  type: ClusterIP
  port: 80
  internalPort: 8080
  healthCheckPath: /

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80

ingress:
  enabled: true

env:
  - name: ENV_TYPE
    value: prod
